# Cors Proxy for Google Apps Scripts doGet / doPosts endpoint

Send request to /<app_id> , get it converted to 

```
https://script.google.com/macros/s/{app_id}/exec
```

* CORS Allow All
* Supports redirects
* JSON ONLY
